# Day 6: персистентная история диалога

Диалог сохраняется между запусками.

## Что нового
- Флаг `--history <файл>` — история диалога в JSON.
- История загружается перед каждым вызовом и сохраняется после.
- `--history ""` отключает персистентность.

## Далее
Day7 — чекпоинт (та же кодовая база). Day8 добавляет учёт токенов и стоимости.
