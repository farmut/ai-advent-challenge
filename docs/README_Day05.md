# Day 5: динамический выбор модели

Модель можно менять на каждый вызов (override `LLM_MODEL` внутри цикла).

## Что нового
- Выбор модели per-call.
- Makefile-цель `run-integration-batch` перебирает три модели Qwen, собирает ответы в `test_output.txt`, затем вызывает экспертную модель для финальной оценки.
- `system_expert.txt` — системный промпт эксперта-оценщика для батч-сравнения моделей.

## Далее
Day6 добавляет персистентную историю диалога.
